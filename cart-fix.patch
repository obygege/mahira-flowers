diff --git a/views/cart.html b/views/cart.html
index 32f0a88..1e2f17f 100644
--- a/views/cart.html
+++ b/views/cart.html
@@ -106,12 +106,56 @@ function prefillFromDraft(){
     document.getElementById('c_cardTo').value = draft.card_to || '';
     document.getElementById('c_cardMessage').value = draft.card_message || '';
     document.getElementById('c_voucherCode').value = draft.voucher_code || '';
+    if(draft.voucher_code && !appliedVoucher) applyVoucherCode(draft.voucher_code);
   }catch(e){/* draft rusak, abaikan */}
 }
 
-function renderCartItems(dict){
+function cartSubtotal(){
+  return cartItems.reduce((s,i)=>s+Number(i.price)*i.quantity,0);
+}
+
+// Render ringkasan total: kalau ada voucher aktif, tampilkan subtotal, potongan,
+// dan total akhir SECARA REAL-TIME di halaman keranjang — supaya angkanya
+// sudah pasti sama persis dengan yang dikirim ke pesanan/WhatsApp nanti.
+function renderSummary(dict){
+  const subtotal=cartSubtotal();
+  if(!cartItems.length){document.getElementById('summary').innerHTML='';return;}
+  if(appliedVoucher){
+    const total=Math.max(0,subtotal-Number(appliedVoucher.discount));
+    document.getElementById('summary').innerHTML=`
+      <div class="total-row"><span>Subtotal</span><span>${money(subtotal)}</span></div>
+      <div class="total-row" style="color:#b3261e;"><span>Voucher (${safe(appliedVoucher.code)})</span><span>-${money(appliedVoucher.discount)}</span></div>
+      <div class="total-row"><strong>${dict.cart_total}</strong><strong>${money(total)}</strong></div>`;
+  }else{
+    document.getElementById('summary').innerHTML=`<div class="total-row"><span>${dict.cart_total}</span><span>${money(subtotal)}</span></div>`;
+  }
+}
+
+// Kalau isi keranjang berubah (qty naik/turun/hapus), voucher yang sudah dipakai
+// perlu dicek ulang ke server (subtotal baru bisa saja jadi di bawah minimal order,
+// atau produknya sudah tidak lagi tercakup voucher). Ini mencegah harga di halaman
+// keranjang beda dengan harga final yang tercatat di pesanan/WhatsApp.
+async function revalidateVoucher(){
+  if(!appliedVoucher)return;
+  const message=document.getElementById('c_voucherMessage');
+  const subtotal=cartSubtotal();
+  try{
+    const response=await fetch('/api/vouchers/validate',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({code:appliedVoucher.code,subtotal})});
+    const result=await response.json();
+    if(!response.ok||!result.success)throw new Error(result.message);
+    appliedVoucher=result.data;
+    if(message)message.textContent=`Voucher aktif. Hemat ${money(result.data.discount)}.`;
+  }catch(error){
+    appliedVoucher=null;
+    if(message)message.textContent=`Voucher dibatalkan: ${error.message}`;
+  }
+}
+
+async function renderCartItems(dict){
   document.getElementById('items').innerHTML=cartItems.map(i=>`<div class="cart-row"><img src="${safe(i.image_url)}" alt="${safe(i.name)}"><div><h3>${safe(i.name)}</h3><div class="price">${money(i.price)}</div></div><div class="qty"><button data-dec="${i.product_id}">-</button><strong>${i.quantity}</strong><button data-inc="${i.product_id}">+</button></div><button class="btn btn-light" data-remove="${i.product_id}" title="Hapus"><i class="fa-solid fa-trash"></i></button></div>`).join('')||`<div class="empty">${dict.cart_empty}</div>`;
-  document.getElementById('summary').innerHTML=cartItems.length?`<div class="total-row"><span>${dict.cart_total}</span><span>${money(cartItems.reduce((s,i)=>s+Number(i.price)*i.quantity,0))}</span></div>`:'';
+  if(!cartItems.length)appliedVoucher=null;
+  await revalidateVoucher();
+  renderSummary(dict);
   document.getElementById('checkoutCard').hidden = !cartItems.length;
   if(cartItems.length) prefillFromDraft();
 }
@@ -152,11 +196,10 @@ document.getElementById('c_deliveryType').addEventListener('change', e=>{
   document.getElementById('c_deliveryFields').hidden = e.target.value !== 'delivery';
 });
 
-document.getElementById('c_voucherCheck').addEventListener('click', async ()=>{
-  const code=document.getElementById('c_voucherCode').value.trim();
+async function applyVoucherCode(code){
   const message=document.getElementById('c_voucherMessage');
   if(!code){message.textContent='Masukkan kode voucher.';return;}
-  const subtotal=cartItems.reduce((s,i)=>s+Number(i.price)*i.quantity,0);
+  const subtotal=cartSubtotal();
   try{
     const response=await fetch('/api/vouchers/validate',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({code,subtotal})});
     const result=await response.json();
@@ -164,6 +207,11 @@ document.getElementById('c_voucherCheck').addEventListener('click', async ()=>{
     appliedVoucher=result.data;
     message.textContent=`Voucher aktif. Hemat ${money(result.data.discount)}.`;
   }catch(error){appliedVoucher=null;message.textContent=error.message;}
+  renderSummary(customerTranslations[customerLang()]);
+}
+
+document.getElementById('c_voucherCheck').addEventListener('click', ()=>{
+  applyVoucherCode(document.getElementById('c_voucherCode').value.trim());
 });
 
 document.getElementById('checkoutSubmit').addEventListener('click', async function(){
@@ -183,13 +231,29 @@ document.getElementById('checkoutSubmit').addEventListener('click', async functi
     card_message: document.getElementById('c_cardMessage').value.trim(),
     voucher_code: appliedVoucher?.code || document.getElementById('c_voucherCode').value.trim()
   };
+  if(!cartItems.length){
+    status.textContent='Keranjang masih kosong, tambahkan produk terlebih dahulu.';
+    return;
+  }
   if(!payload.delivery_date||!payload.sender_name||!payload.sender_phone||(deliveryType==='delivery'&&!payload.delivery_address)){
     status.textContent='Mohon lengkapi data pengiriman dan data pengirim terlebih dahulu.';
     return;
   }
   button.disabled=true; button.textContent='Memproses...'; status.textContent='';
   try{
-    const result=await customerFetch('/api/cart/checkout',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(payload)});
+    let result;
+    if(isGuest){
+      // Checkout tanpa login: kirim langsung item keranjang tamu (produk + qty) supaya
+      // server menghitung ulang harga & voucher dari database — hasilnya dipakai juga
+      // untuk teks WhatsApp, jadi pasti sama dengan yang tampil di halaman keranjang.
+      payload.items = cartItems.map(i=>({ product_id: i.product_id, quantity: i.quantity }));
+      const response = await fetch('/api/orders', { method:'POST', headers:{'Content-Type':'application/json'}, body: JSON.stringify(payload) });
+      result = await response.json();
+      if(!response.ok || !result.success) throw new Error(result.message || 'Gagal membuat pesanan');
+      saveGuestCart([]);
+    } else {
+      result = await customerFetch('/api/cart/checkout',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(payload)});
+    }
     localStorage.removeItem('mahira_checkout_draft');
     status.textContent='Pesanan dibuat! Mengarahkan ke WhatsApp admin untuk konfirmasi transfer...';
     setTimeout(()=>{ location.href = result.whatsapp_url || `/payment/${result.order_number}`; }, 600);
