const translations = {
  id: {
    nav_home: "Beranda",
    nav_categories: "Kategori",
    nav_custom: "Pesanan Custom",
    nav_about: "Tentang Kami",
    nav_contact: "Kontak",
    cat_fresh: "Bunga Segar",
    cat_bouquets: "Buket",
    cat_vases: "Vas Bunga",
    cat_box: "Flower Box",
    cat_gifts: "Hadiah & Hampers",
    cat_dried: "Bunga Kering",
    cat_occasion: "Acara & Momen",
    cat_wedding: "Bunga Pernikahan",
    cat_hand: "Buket Tangan",
    cat_standing: "Bunga Standing",
    cat_products: "Semua Produk",
    search_placeholder: "Cari produk...",
    account_title: "Masuk ke akun Anda",
    account_email: "Email",
    account_password: "Kata Sandi",
    account_login: "Masuk",
    cart_title: "Keranjang Anda",
    cart_items: "item",
    cart_checkout: "Checkout",
    hero_title: "Bunga Elegan untuk Setiap Momen",
    hero_desc: "Rangkaian bunga mewah buatan tangan yang menghadirkan keindahan kelopak terbaik alam langsung ke rumah Anda atau orang tersayang.",
    hero_btn: "Belanja Sekarang",
    hero_wedding_title: "Koleksi Pernikahan Eksklusif",
    hero_wedding_desc: "Jadikan hari istimewa Anda tak terlupakan dengan desain bunga pernikahan khusus dari kami.",
    hero_wedding_btn: "Lihat Selengkapnya",
    hero_gift_title: "Flower Box Premium",
    hero_gift_desc: "Kejutkan orang tersayang dengan flower box mewah yang indah dan tahan lama.",
    hero_gift_btn: "Lihat Koleksi",
    new_arrivals_title: "Produk Terbaru",
    new_arrivals_desc: "Desain bunga segar dan menawan yang baru hadir dalam koleksi kami",
    cat_section_title: "Belanja per Kategori",
    cat_section_desc: "Jelajahi koleksi pilihan desain botani premium kami",
    bestseller_title: "Produk Terlaris Kami",
    bestseller_desc: "Buket buatan tangan favorit para pelanggan kami",
    view_all: "Lihat Semua",
    badge_bestseller: "TERLARIS",
    badge_popular: "POPULER",
    badge_exquisite: "EKSKLUSIF",
    badge_new: "BARU",
    add_cart: "Tambah ke Keranjang",
    shopee: "Shopee",
    product_midnight: "Keanggunan Lily Tengah Malam",
    product_orchid: "Harmoni Anggrek Putih",
    product_daisy: "Keranjang Daisy Klasik",
    product_amour: "Buket Mawar Coral Amour",
    product_peach: "Vas Peach Menawan",
    product_blossom: "Flower Box Beautiful Happiness",
    custom_title: "Rangkaian Custom & Pesanan Khusus",
    custom_desc: "Punya visi unik? Florist ahli kami berkolaborasi langsung dengan Anda untuk membuat desain khusus, rangkaian skala besar, dan centerpiece acara yang megah.",
    custom_btn: "Konsultasi dengan Florist",
    feat1_title: "Pengiriman Instan Gratis",
    feat1_desc: "Pengiriman instan tanpa biaya untuk pesanan lokal di atas Rp 1.500.000",
    feat2_title: "Jaminan Kesegaran",
    feat2_desc: "Dipasok setiap hari dari petani lokal dan global pilihan",
    feat3_title: "Pengiriman di Hari yang Sama",
    feat3_desc: "Pesan sebelum jam 2 siang untuk layanan pengiriman di hari yang sama",
    feat4_title: "Checkout Aman",
    feat4_desc: "Pemrosesan pembayaran canggih yang menjaga keamanan data Anda",
    testi_title: "Kata Pelanggan Kami",
    role1: "Hadiah Anniversary",
    role2: "Pesanan Acara Khusus",
    role3: "Langganan Mingguan",
    quote1: "Buket Mawar Coral Amour benar-benar menakjubkan! Harum seperti surga dan bertahan lebih dari dua minggu. Kualitas pengerjaan premium.",
    quote2: "Pelayanan pelanggan luar biasa! Mereka langsung membantu permintaan perubahan custom dan mengirimkannya tepat waktu. Mahira memang terbaik.",
    quote3: "Rangkaian vasnya benar-benar karya seni. Semua tamu bertanya di mana saya membelinya. Warna peach-nya sangat cocok dengan ruang tamu saya.",
    insta_title: "Ikuti Kami di Instagram",
    footer_desc: "Florist bunga segar premium yang menghadirkan rangkaian bunga elegan ke depan pintu Anda. Di mana setiap kelopak punya cerita.",
    footer_categories: "KATEGORI",
    footer_care: "LAYANAN PELANGGAN",
    footer_newsletter: "GABUNG NEWSLETTER KAMI",
    footer_boxes: "Flower Box",
    care_contact: "Hubungi Kami",
    care_delivery: "Info Pengiriman",
    care_faq: "FAQ",
    care_terms: "Syarat & Ketentuan",
    care_return: "Kebijakan Pengembalian",
    newsletter_desc: "Berlangganan untuk mendapatkan diskon, tips bunga, dan info terbaru.",
    newsletter_placeholder: "Alamat email Anda",
    newsletter_btn: "Berlangganan",
    footer_rights: "© 2026 Mahira Flowers. Seluruh Hak Cipta Dilindungi.",
    modal_product: "Nama Produk",
    form_delivery_title: "Detail Pickup & Pengiriman",
    form_method: "Metode pesanan",
    delivery: "Pengiriman",
    pickup: "Pickup",
    form_area_note: "Area Bali & Jakarta, termasuk Tangerang, Depok, Bekasi, dan Cibubur.",
    form_address: "Alamat lengkap pengiriman",
    form_maps: "Link Google Maps",
    optional: "(opsional)",
    form_date: "Tanggal",
    form_time: "Waktu",
    time_morning: "09.00 - 14.00",
    time_afternoon: "15.00 - 17.00",
    form_time_note: "Pesanan di hari yang sama tersedia untuk jadwal 15.00 - 17.00. Pesanan setelah pukul 15.00 dapat diproses keesokan harinya.",
    form_sender_title: "Detail Pengirim & Kartu",
    sender_name: "Nama pengirim",
    sender_phone: "Nomor telepon pengirim",
    card_message: "Pesan pada kartu",
    proceed_order: "Lanjutkan Pesanan",
    address_placeholder: "Jalan, nomor rumah, kecamatan, kota",
    maps_placeholder: "https://maps.google.com/...",
    sender_name_placeholder: "Nama pengirim",
    sender_phone_placeholder: "Nomor telepon pengirim",
    from_placeholder: "Dari:",
    to_placeholder: "Untuk:",
    message_placeholder: "Pesan:",
    footer_intro: "Importir bunga artificial terpercaya dengan koleksi premium, harga terjangkau, dan siap kirim ke seluruh Indonesia.",
    footer_cat_artificial: "Bunga Artificial",
    footer_cat_orchid: "Anggrek Phalaenopsis K9",
    footer_cat_flowerbox: "Flower Box",
    footer_cat_bouquet: "Bouquet",
    footer_cat_all: "Semua Koleksi",
    footer_link_admin: "Hubungi Admin",
    footer_link_location: "Info Lokasi",
    footer_link_shipping: "Info Pengiriman",
    footer_link_return: "Kebijakan Pengembalian",
    footer_contact_title: "KONTAK",
    footer_open_maps: "Buka alamat di Google Maps",
    footer_hours: "Setiap hari, 09.00 - 17.00",
    about_eyebrow: "Tentang Mahira Flowers",
    about_title: "Bunga yang dirangkai untuk cerita yang berarti",
    about_desc: "Kami membantu Anda menyampaikan perhatian melalui rangkaian bunga segar, custom bouquet, dan flower box yang dirancang dengan teliti oleh florist kami.",
    about_contact_link: "Hubungi kami",
    about_fact_fresh: "Bunga pilihan setiap hari",
    about_fact_custom: "Rangkaian sesuai momen",
    about_fact_care: "Respon florist yang personal",
    voucher_label: "Voucher diskon",
    voucher_apply: "Pakai",
    voucher_placeholder: "Masukkan kode voucher",
    added_title: "Ditambahkan ke Keranjang",
    added_desc: "Cek keranjang untuk atur jumlah, tambah produk lain, lalu checkout untuk lanjut ke pembayaran.",
    view_cart_btn: "Lihat Keranjang",
    add_more_btn: "Tambah Produk Lain",
    custom_submit_btn: "Kirim Request Custom",
    login_prompt_title: "Simpan ke Keranjang",
    login_prompt_desc: "Disarankan login agar keranjang Anda tersimpan di akun. Anda tetap bisa melanjutkan tanpa login.",
    login_prompt_yes: "Login Dulu",
    login_prompt_no: "Lanjutkan Tanpa Login"
  }
};

const dynamicText = {
  id: {
    sending_request: "Mengirim request...",
    custom_failed: "Request gagal dikirim",
    voucher_enter_code: "Masukkan kode voucher.",
    voucher_active: amount => `Voucher aktif. Hemat ${amount}.`,
    fill_required: "Mohon lengkapi data pengiriman dan data pengirim terlebih dahulu.",
    adding: "Menambahkan...",
    cart_add_failed: "Gagal menambah ke keranjang"
  },
  en: {
    sending_request: "Sending request...",
    custom_failed: "Failed to send request",
    voucher_enter_code: "Please enter a voucher code.",
    voucher_active: amount => `Voucher active. You saved ${amount}.`,
    fill_required: "Please complete the delivery and sender details first.",
    adding: "Adding...",
    cart_add_failed: "Failed to add to cart"
  }
};
function curLang() { return document.documentElement.getAttribute('lang') || localStorage.getItem('mahira-lang') || 'id'; }
function dt(key) { return dynamicText[curLang()][key]; }

function initLanguageSwitch() {
  const buttons = document.querySelectorAll('.lang-btn');

  function setLanguage(lang) {
    // Re-query every time (not cached once) so elements added later —
    // e.g. Swiper's loop-mode duplicate slides, or newly rendered product
    // cards — are always caught, not just the ones present at page load.
    const i18nEls = document.querySelectorAll('[data-i18n]');
    const placeholderEls = document.querySelectorAll('[data-i18n-placeholder]');

    i18nEls.forEach(el => {
      if (!el.dataset.en) el.dataset.en = el.textContent;
      const key = el.getAttribute('data-i18n');
      if (lang === 'id' && translations.id[key]) {
        el.textContent = translations.id[key];
      } else {
        el.textContent = el.dataset.en;
      }
    });
    placeholderEls.forEach(el => {
      if (!el.dataset.enPlaceholder) el.dataset.enPlaceholder = el.getAttribute('placeholder') || '';
      const key = el.getAttribute('data-i18n-placeholder');
      if (lang === 'id' && translations.id[key]) {
        el.setAttribute('placeholder', translations.id[key]);
      } else {
        el.setAttribute('placeholder', el.dataset.enPlaceholder);
      }
    });
    buttons.forEach(btn => btn.classList.toggle('active', btn.dataset.lang === lang));
    document.documentElement.setAttribute('lang', lang);
    localStorage.setItem('mahira-lang', lang);
    document.dispatchEvent(new CustomEvent('mahira-lang-changed', { detail: { lang } }));
  }

  buttons.forEach(btn => {
    btn.addEventListener('click', () => setLanguage(btn.dataset.lang));
  });

  // Swiper (loop:true) builds its duplicate slides asynchronously right
  // after init, so re-apply the language shortly after load to make sure
  // those clones pick up the correct text too.
  const savedLang = localStorage.getItem('mahira-lang') || 'id';
  setLanguage(savedLang);
  setTimeout(() => setLanguage(localStorage.getItem('mahira-lang') || 'id'), 300);

  window.mahiraSetLanguage = setLanguage;
}

async function loadCategories() {
  const navList = document.getElementById('navCategoryList');
  const shopGrid = document.getElementById('shopByCategoryList');
  if (!navList && !shopGrid) return;
  try {
    const res = await fetch('/api/categories');
    const data = await res.json();
    const categories = data.success ? data.data : [];

    if (navList) {
      const allProductsLi = navList.querySelector('li');
      const items = categories.map(c => `<li><a href="/category/${encodeURIComponent(c.slug)}">${safeText(c.name)}</a></li>`).join('');
      navList.innerHTML = items + (allProductsLi ? allProductsLi.outerHTML : '');
    }

    if (shopGrid) {
      shopGrid.innerHTML = categories.map(c => `
        <a class="category" href="/category/${encodeURIComponent(c.slug)}">
          <img src="${safeText(c.image_url || '/images/logo.png')}" alt="${safeText(c.name)}" loading="lazy" decoding="async" onerror="this.onerror=null;this.src='/images/logo.png'">
          <span>${safeText(c.name)}</span>
        </a>`).join('');
    }
  } catch (e) {
    console.error('Gagal memuat kategori', e);
  }
}
function safeText(value) {
  return String(value ?? '').replace(/[&<>"']/g, c => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;' }[c]));
}

function initCategoryDropdown() {
  const dropdown = document.querySelector('.nav-dropdown');
  if (!dropdown) return;
  const trigger = dropdown.querySelector(':scope > a');

  function setDropdownState(isOpen) {
    dropdown.classList.toggle('open', isOpen);
    const menu = dropdown.querySelector('.dropdown-menu');
    if (!menu) return;
    menu.style.removeProperty('opacity');
    menu.style.removeProperty('visibility');
    menu.style.removeProperty('transform');
  }

  trigger.addEventListener('click', (e) => {
    if (window.innerWidth <= 768) {
      e.preventDefault();
      setDropdownState(!dropdown.classList.contains('open'));
    } else {
      setDropdownState(true);
    }
  });
  dropdown.addEventListener('mouseenter', () => setDropdownState(true));
  dropdown.addEventListener('mouseleave', () => {
    if (!dropdown.classList.contains('open')) setDropdownState(false);
  });

  function closeOnOutside(e) {
    if (!dropdown.contains(e.target)) {
      setDropdownState(false);
    }
  }

  document.addEventListener('click', closeOnOutside);
}

function initAccountState() {
  const trigger = document.getElementById('accountTrigger');
  const box = document.getElementById('accountBox');
  if (!trigger || !box) return;
  const token = typeof customerToken === 'function' ? customerToken() : localStorage.getItem('customer_token');
  const user = typeof customerUser === 'function' ? customerUser() : JSON.parse(localStorage.getItem('customer_user') || 'null');
  if (!token || !user) return; // Guest: keep the default login form as-is.

  trigger.classList.add('is-logged-in');
  trigger.setAttribute('aria-label', user.name ? `Masuk sebagai ${user.name}` : 'Sudah masuk');
  trigger.querySelector('i')?.classList.replace('fa-regular', 'fa-solid');

  const lang = curLang();
  const name = (typeof safeText === 'function' ? safeText(user.name || '') : (user.name || ''));
  box.innerHTML = `
    <p class="account-user-pill"><i class="fa-solid fa-circle-check"></i> ${name || (lang === 'id' ? 'Akun Anda' : 'Your account')}</p>
    <div class="account-logged-links">
      <a href="/favorites">${lang === 'id' ? 'Favorit' : 'Favorites'}</a>
      <a href="/cart">${lang === 'id' ? 'Keranjang' : 'Cart'}</a>
      <a href="#" class="logout-link" id="accountLogoutLink">${lang === 'id' ? 'Keluar' : 'Logout'}</a>
    </div>`;
  document.getElementById('accountLogoutLink')?.addEventListener('click', (e) => {
    e.preventDefault();
    localStorage.removeItem('customer_token');
    localStorage.removeItem('customer_user');
    showToast(lang === 'id' ? 'Anda telah keluar.' : 'You have been logged out.', 'success');
    setTimeout(() => location.reload(), 600);
  });
}

function initTopbarActions() {
  const searchItem = document.querySelector('.search-item');
  const accountItem = document.querySelector('.account-item');
  const cartItem = document.querySelector('.cart-item');
  const panels = [searchItem, accountItem, cartItem].filter(Boolean);

  function closeAll(except) {
    panels.forEach(p => { if (p !== except) p.classList.remove('open'); });
  }

  function wireTrigger(item) {
    if (!item) return;
    const trigger = item.querySelector('.icon-trigger');
    trigger.addEventListener('click', (e) => {
      e.stopPropagation();
      const isOpen = item.classList.contains('open');
      closeAll(item);
      item.classList.toggle('open', !isOpen);
      if (item === searchItem && !isOpen) {
        const input = item.querySelector('input');
        if (input) setTimeout(() => input.focus(), 50);
      }
    });
  }
  panels.forEach(wireTrigger);

  document.addEventListener('click', (e) => {
    panels.forEach(p => {
      if (p && !p.contains(e.target)) p.classList.remove('open');
    });
  });

  // Search submit
  const searchInput = document.querySelector('.search-box input');
  const searchGo = document.querySelector('.search-go');
  function doSearch() {
    const term = searchInput.value.trim();
    if (!term) return;
    document.querySelectorAll('.product-card').forEach(card => {
      const name = card.querySelector('h3')?.textContent.toLowerCase() || '';
      const match = name.includes(term.toLowerCase());
      card.style.display = match ? '' : 'none';
    });
    document.querySelector('.products')?.scrollIntoView({ behavior: 'smooth', block: 'center' });
    searchItem.classList.remove('open');
  }
  searchGo?.addEventListener('click', doSearch);
  searchInput?.addEventListener('keydown', (e) => { if (e.key === 'Enter') doSearch(); });

  // Login langsung dari navbar (dropdown akun)
  const accountBtn = document.querySelector('.account-box .btn');
  accountBtn?.addEventListener('click', async () => {
    const lang = document.documentElement.getAttribute('lang');
    const emailInput = document.querySelector('.account-box input[type="email"]');
    const passwordInput = document.querySelector('.account-box input[type="password"]');
    const email = emailInput?.value.trim();
    const password = passwordInput?.value;
    if (!email || !password) {
      showToast(lang === 'id' ? 'Isi email dan password terlebih dahulu.' : 'Please fill in email and password.', 'warning');
      return;
    }
    try {
      const res = await fetch('/api/auth/login', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ email, password })
      });
      const data = await res.json();
      if (!res.ok || !data.success) throw new Error(data.message || (lang === 'id' ? 'Login gagal' : 'Login failed'));
      localStorage.setItem('customer_token', data.token);
      localStorage.setItem('customer_user', JSON.stringify(data.user));
      showToast(lang === 'id' ? `Selamat datang kembali, ${data.user.name}!` : `Welcome back, ${data.user.name}!`, 'success');
      accountItem.classList.remove('open');
      setTimeout(() => location.reload(), 700);
    } catch (err) {
      showToast(err.message, 'error');
    }
  });

  // Cart checkout (demo)
  const cartBtn = document.querySelector('.cart-box .btn');
  cartBtn?.addEventListener('click', () => {
    const lang = document.documentElement.getAttribute('lang');
    alert(lang === 'id' ? 'Menuju halaman checkout...' : 'Heading to checkout...');
    cartItem.classList.remove('open');
  });
}

document.addEventListener('DOMContentLoaded', () => {
  initLanguageSwitch();
  loadCategories();
  initCategoryDropdown();
  initTopbarActions();
  initAccountState();

  const deliveryType = document.querySelector('#deliveryType');
  const deliveryFields = document.querySelector('#deliveryFields');
  const deliveryAddress = document.querySelector('#deliveryAddress');
  const mapsLink = document.querySelector('#mapsLink');

  function updateDeliveryFields() {
    const isDelivery = deliveryType?.value === 'delivery';
    if (deliveryFields) deliveryFields.hidden = !isDelivery;
    if (deliveryAddress) deliveryAddress.required = isDelivery;
  }

  deliveryType?.addEventListener('change', updateDeliveryFields);
  updateDeliveryFields();

  let cartCount = 3;
  let wishlistCount = 0;
  const badge = document.querySelector('.cart-badge');
  const wishlistBadge = document.querySelector('.wishlist-badge');
  const cartSummary = document.querySelector('.cart-summary span');

  function updateCartSummary() {
    if (!cartSummary) return;
    const lang = document.documentElement.getAttribute('lang');
    cartSummary.textContent = lang === 'id' ? `${cartCount} item` : `${cartCount} item${cartCount === 1 ? '' : 's'}`;
  }
  updateCartSummary();
  document.addEventListener('mahira-lang-changed', updateCartSummary);

  // Cart and favorite actions are handled by customer.js and persisted in MySQL.

  // Scroll reveal animation
  const revealEls = document.querySelectorAll('.reveal');
  const observer = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        entry.target.classList.add('visible');
        observer.unobserve(entry.target);
      }
    });
  }, { threshold: 0.15 });

  revealEls.forEach(el => observer.observe(el));
});
